{
  "name": "star-collector",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r game.js dist/"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
